/**
 * User-visible app name in chrome (e.g. top bar). Use the same placeholder as the bare template:
 * the CLI replaces `ReExpoApp` with the project name for both Expo and bare.
 * Keep aligned with `app.config.ts` flavor names if you customize.
 */
export const APP_DISPLAY_NAME = "ReExpoApp";
